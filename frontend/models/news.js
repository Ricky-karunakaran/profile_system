class News {
  constructor({ title, content, url, create, img }) {
    this.title = title;
    this.content = content;
    this.url = url;
    this.create = create;
    this.img = img;
  }
}

export default News;
